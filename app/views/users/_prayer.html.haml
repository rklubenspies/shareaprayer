.request
	.profilepic
		= image_tag @user.profile.image
	.details
		.text
			= prayer.prayer
		.timestamp
			Posted #{timeago(prayer.created_at)}&nbsp;
			= link_to "Leave a Comment", user_prayer_path(@user.screenname, prayer.id)
			- if can? :destroy, Prayer
				&nbsp;&nbsp;
				= link_to 'Destroy', user_prayer_path(@user.screenname, prayer.id), :confirm => 'Are you sure?', :method => :delete
.clear
.divider17
	%hr
.clear