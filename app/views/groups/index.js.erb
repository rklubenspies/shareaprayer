$('table tbody').html("<%=escape_javascript(render :partial => 'group_table_item', :collection => @groups)%>");
$('.paginator').html('<%= escape_javascript(paginate(@groups, :remote => true, :theme => "github").to_s) %>');